/*
Middle layer between UI and database. Interface (CLI/GUI) asks it to do things, and
PackageService figures out logic and talks to repository
*/

public class PackageService {
    // TODO: Add methods for:
    //  - addPackage(...)      -> creates new Package w generated tracking ID, saved
    //  - updateStatus(...)    -> finds a package and updates its status, logs TrackingEvent
    //  - findById(...)        -> returns single Package by tracking ID
    //  - getAllPackages(...)  -> returns all packages, displayed in sortable table
    //  - removePackage(...)   -> deletes a package by tracking ID
}